export class flowRequest {
  allow_source = true;
  fast_bread = true;
  P: number = Math.floor(Math.random() * 1e7);
  player_backend = "mediaplayer";
  playlist_include_framerate = true;
  reassignments_supported = false;
  sig: string = "";
  supported_codecs = "avc1";
  token: string = "";
}
